import { IotaClient } from '@iota/iota-sdk/client';
import type { NetworkConfig } from '../hooks/networkConfig.js';
type NetworkConfigs<T extends NetworkConfig | IotaClient = NetworkConfig | IotaClient> = Record<string, T>;
export interface IotaClientProviderContext {
    client: IotaClient;
    networks: NetworkConfigs;
    network: string;
    config: NetworkConfig | null;
    selectNetwork: (network: string) => void;
}
export declare const IotaClientContext: import("react").Context<IotaClientProviderContext | null>;
export type IotaClientProviderProps<T extends NetworkConfigs> = {
    createClient?: (name: keyof T, config: T[keyof T]) => IotaClient;
    children: React.ReactNode;
    networks?: T;
    onNetworkChange?: (network: keyof T & string) => void;
} & ({
    defaultNetwork?: keyof T & string;
    network?: never;
} | {
    defaultNetwork?: never;
    network?: keyof T & string;
});
export declare function IotaClientProvider<T extends NetworkConfigs>(props: IotaClientProviderProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
